we propose a new distributed, fault-tolerant peer-to-peer index structure for resource discovery applications called the p-tree. p-trees e ciently support range queries in addition to equality queries.
